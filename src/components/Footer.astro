---
const currentYear = new Date().getFullYear()
import Container from './Container.astro'
import { site } from '@/config'
import { Icon } from 'astro-icon/components'
---

<footer>
	<Container>
		<article class="grid grid-cols-1 gap-8 md:grid-cols-3 py-8">
			<div class="flex flex-col gap-4">
				<div>
					<h4 class="text-base font-bold mb-2">Entrega</h4>
					<p>Delivery en Lima y envíos por agencia sugeridad a nivel nacional</p>
				</div>
				<div>
					<h4 class="text-base font-bold mb-2">Dirección</h4>
					<p>{site.address}</p>
				</div>
			</div>
			<div class="flex flex-col gap-4">
				<div>
					<h4 class="text-base font-bold mb-2">Contátenos</h4>
					<p class="mb-2">{site.email}</p>
					<p>+51 {site.phone}</p>
				</div>
				<div>
					<h4 class="text-base font-bold mb-2">Horario de Atencion</h4>
					<p>Lunes a Sábado de 10:00AM - 07:00PM</p>
				</div>
			</div>
			<div class="flex flex-col gap-4">
				<div>
					<h4 class="text-base font-bold mb-2">Aceptamos</h4>
					<img src="/assets/images/pay-methods.webp" />
				</div>
				<div>
					<h4 class="text-base font-bold mb-2">Redes Sociales</h4>
					<div class="flex items-center gap-2">
						{
							Object.entries(site.socialLinks).map(([key, value]) => {
								return (
									<a href={value} class="hover:scale-125 transition-transform duration-200">
										<Icon name={key} size={25} />
									</a>
								)
							})
						}
					</div>
				</div>
			</div>
		</article>
		<aricle class="">
			<div class="container flex flex-col items-center justify-center mb-8">
				<p class="text-center mt-4 text-base">
					&nbsp;{site.name}©{currentYear}
				</p>
				<p class="text-sm text-center flex items-center">
					Hecho con mucho <span class="inline">
						<Icon name="heart" size={24} />
					</span>
				</p>
			</div>
		</aricle>
	</Container>
</footer>
<style>
	[data-icon='heart'] {
		animation: pulse 800ms ease infinite;
		margin-left: 4px;
	}

	@keyframes pulse {
		0%,
		100% {
			transform: scale(1);
			text-shadow: 0 0 5px rgba(255, 107, 107, 0.5);
		}
		50% {
			transform: scale(1.2);
			text-shadow: 0 0 15px rgba(255, 61, 61, 0.7);
		}
	}
</style>
